interface:
  display_name: "Upgrade Spring Boot"
  short_description: "Plan and execute Spring Boot upgrades"
  default_prompt: "Use $upgrade-springboot to plan and execute a Spring Boot upgrade with official docs checks, dependency compatibility review, phased PR strategy, and Gradle test verification."

policy:
  allow_implicit_invocation: false
