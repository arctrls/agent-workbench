interface:
  display_name: "dev-deploy"
  short_description: "Reset dev to origin, merge current branch, push, and return"
  default_prompt: "Use $dev-deploy to reset dev to origin/dev, merge the current branch, push, and return to the original branch."
